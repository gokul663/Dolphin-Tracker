{
  "buildCommand": "cd frontend && CI=false yarn install --legacy-peer-deps && CI=false yarn build",
  "outputDirectory": "frontend/build",
  "framework": "create-react-app",
  "installCommand": "cd frontend && yarn install --legacy-peer-deps",
  "build": {
    "env": {
      "NODE_VERSION": "18"
    }
  }
}